name = "nutrirun-pro"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

[vars]
# Variables públicas (no secretos) van aquí.
# La GEMINI_API_KEY se configura como Secret en el dashboard de Cloudflare Pages,
# NO la pongas aquí en texto plano.
